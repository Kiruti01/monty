#include "monty.h"
/**
 * f_stack - prints the top
 * @head: stack head
 * @counter: line_number
 * @bus: ptr to bus struct
 * Return: no return
 */
void f_stack(stack_t **head, unsigned int counter, bus_t *bus)
{
	(void)head;
	(void)counter;
	bus->lifi = 0;
}
